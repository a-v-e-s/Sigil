Make the inkblots colorized!!!

make main gui look better.
change it so that different options appear or disappear depending on the chosen mode.

clean up the codebase. it is an absolute mess.
make the repo more professional looking and make the src folder scripts accessible from the top level directory.